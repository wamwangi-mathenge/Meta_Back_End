# read() => Returns the entire contents of the file as a string that will contain all the characters.
# with open('newfile.txt', 'r') as file:
    # print(file.read())
    # You can also pass in an integer to return only the specified characters in the file
    # print(file.read(40))
    
# readline() => Returns a single line as a string.
    # print(file.readline())
    # You can also pass in an integer to return a specified number of characters in a single line
    # print(file.readline(6))
    
# readlines() => Returns the entire contents of the file and then returns it in an ordered list
    # print(file.readlines())